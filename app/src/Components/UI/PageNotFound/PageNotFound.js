import { StyledPageNotFound } from './styled';

const PageNotFound = () => (
    <StyledPageNotFound>
        <strong> Page not found </strong>
    </StyledPageNotFound>
);

export default PageNotFound;
