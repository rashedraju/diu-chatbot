import React from 'react';
import { IconWrapper, StyledBotIcon, StyledPersonIcon } from './styled';

export const BotIcon = () => (
    <IconWrapper>
        <StyledBotIcon />
    </IconWrapper>
);

export const PersonIcon = () => (
    <IconWrapper>
        <StyledPersonIcon />
    </IconWrapper>
);
